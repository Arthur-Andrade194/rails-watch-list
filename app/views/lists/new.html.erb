<div class="container">
  <h1>Create a List</h1>
  <%= simple_form_for [@movie, @list] do |f| %>
    <%= f.input :name %>

    <%= f.submit 'Add List', class: 'btn btn-danger mt-2' %>
  <% end %>
</div>
